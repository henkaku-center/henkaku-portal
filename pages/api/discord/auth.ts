import cookie from "cookie";

const {
    DISCORD_SERVER_ID,
    DISCORD_CLIENT_SECRET,
    DISCORD_CLIENT_ID,
    APP_URI,
    JWT_SECRET,
    COOKIE_NAME, 
} = process.env;

async function exchangeToken(code){
    
}